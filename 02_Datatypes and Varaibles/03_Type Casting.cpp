#include<iostream>
using namespace std;

int main(){
    //type cating -> from one datatype to another datatype.
    //ascii table

    int a = 'a';
    cout<< a << endl;

    char ch = 97;
    cout << ch << endl;

    // If you are going to store number which is not in range of another datatype then it stores that number and print the value which is present at last byte.
    // int max value = 2 ki power 32 -1 and min value = 0 (positive(unsigned) ke case me )
    // char max value = 2 ki power 8 -1 and min value =0.

    // char ch1 = 123456;
    // cout<<ch1<<endl;

}