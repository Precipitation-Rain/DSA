/*
#include<iostream>
using namespace std;

int main(){
    // int num = 2;

    // cout<<endl;
    // switch(num){
    //     case 1: cout<<"First"<<endl;
    //             break;

    //     case 2:cout<<"Second"<<endl;
    //             break;

    //     default :cout<<"It is default case"<<endl;

    // }
      char ch = '1';

    cout<<endl;
    switch(ch){
        case '1': cout<<"First"<<endl;
                break;
                //agar isme break use nahi kiya to  niche wale sare cases execute ho jayenge(agar unme break nahi hain to)
                //agar un me se kisi ek ke bhi pass break hain to wahi break ho jayega .default nahi hoga
                //the case which is closest to defualt ,that only case execute the default also when the case don't use break.

        case '2':cout<<"Second"<<endl;
                break;

         case '3':cout<<"Third"<<endl;
                break;

         default :cout<<"It is default case"<<endl;

    }
    cout<<endl;

    // we can do nested switch also.But hame uski need nahi padegi.

    //using continue inside switch is not valid;
    //gives an error

    
}

*/
